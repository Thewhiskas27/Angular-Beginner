import { ColorTasksDirective } from './color-tasks.directive';

describe('ColorTasksDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTasksDirective();
    expect(directive).toBeTruthy();
  });
});
